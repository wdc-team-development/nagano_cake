<h1>商品詳細ページ</h1>

<h4>左側に商品画像</h4>
<%= attachment_image_tag(@item, :image, :fill, 100, 100) %>

<%= @item.name %>

<%= @item.introduction %>

<%= @item.genre_id %>

<%= @item.price %><%= "円" %>

<%= @item.is_active %>

<%= link_to '編集する',  edit_admin_item_path(@item.id) %>